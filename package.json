{
  "name": "geovibe-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Satellite environmental monitoring and change detection with a trained random forest model.",
  "scripts": {
    "start": "node server.js",
    "train": "node scripts/train.js && node scripts/train-risk.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
